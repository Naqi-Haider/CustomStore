<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Starter Theme</title>
    {{ content_for_header }}

    {% comment %} For the cart-drawer section rendering api {% endcomment %}
    {{ 'cart-drawer.css' | asset_url | stylesheet_tag }}

    <script src="{{ 'global.js' | asset_url }}" defer></script>

</head>
<body>
    {% render 'cart-drawer' %}
    {{ content_for_layout }}
</body>
</html>
